= link_to '&laquo; back'.html_safe, root_path, class: 'back'

%h1
  Your bookmarks from 
  = @domain.to_s

.search
  %input#search{placeholder: 'Search', data: {container: '.bookmarks', item: '.bookmark', text: '.url'}}

.bookmarks
  - @domain.bookmarks.each do |bookmark|
    = render bookmark